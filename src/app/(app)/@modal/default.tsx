// Default render for the @modal parallel slot when no intercepting route is
// active. Rendering null means the slot contributes nothing to normal pages.
export default function ModalDefault() {
  return null;
}
